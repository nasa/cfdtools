!     Dummy IMSL routines, etc.

      subroutine ugetio (m, n, lun)
      end
      subroutine ICSEVU
!     write (6, '(/, a)') ' IMSL routines are no longer usable.'
      end
      subroutine ICSFKU
      write (6, '(/, a)') ' IMSL routines are no longer usable.'
      end
      subroutine ICSMOU
      write (6, '(/, a)') ' IMSL routines are no longer usable.'
      end
      subroutine ICSSCU
      write (6, '(/, a)') ' IMSL routines are no longer usable.'
      end
      subroutine ICSVKU
      write (6, '(/, a)') ' IMSL routines are no longer usable.'
      end
      subroutine IQHSCU
      write (6, '(/, a)') ' IMSL routines are no longer usable.'
      end
      subroutine DCSEVU
      write (6, '(/, a)') ' IMSL routines are no longer usable.'
      end
      subroutine FSARBE
      write (6, '(/, a)') ' FSARBE is missing.'
      end
      subroutine FSARBN
      write (6, '(/, a)') ' FSARBN is missing.'
      end
      subroutine FSARBU
      write (6, '(/, a)') ' FSARBU is missing.'
      end
